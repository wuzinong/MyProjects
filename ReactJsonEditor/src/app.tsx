import { BrowserRouter, Route, Routes } from 'react-router-dom'
import { Layout } from 'pages'

const App = () => {
	return (
		<BrowserRouter>
			<Routes>
				<Route></Route>
			</Routes>
		</BrowserRouter>
	)
}
